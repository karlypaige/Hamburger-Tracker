<h1>Hamburgers!</h1>

<h2>Hamburgers that are not eaten!</h2>

<ul>
  {{#each hamburgers}}
    {{#unless devoured}}
      {{> hamburgers/hamburger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Hamburgers that are eaten!</h2>

<ul>
  {{#each hamburgers}}
    {{#if devoured}}
      {{> hamburgers/hamburger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Hamburger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Hamburger:</label>
    <input type="text" id="ca" name="burger">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Not yet eaten
  </div>

  <button type="submit">Add Hamburger</button>
</form>
